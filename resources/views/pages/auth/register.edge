<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
        Register
    </title>

    <link rel="preconnect" href="https://fonts.bunny.net" />
    <link href="https://fonts.bunny.net/css?family=instrument-sans:400,400i,500,500i,600,600i,700,700i"
        rel="stylesheet" />

    <style>
        :root {
            --sand-1: #fdfdfc;
            --sand-2: #f9f9f8;
            --sand-3: #f1f0ef;
            --sand-4: #e9e8e6;
            --sand-5: #e2e1de;
            --sand-6: #dad9d6;
            --sand-7: #cfceca;
            --sand-8: #bcbbb5;
            --sand-9: #8d8d86;
            --sand-10: #82827c;
            --sand-11: #63635e;
            --sand-12: #21201c;
        }
    </style>

    <script src="https://cdn.tailwindcss.com">

    </script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ["Instrument Sans", "sans-serif"]
                    },
                    colors: {
                        primary: {
                            DEFAULT: "#5A45FF",
                            lighter: "#a599ff"
                        },
                        sand: {
                            1: "var(--sand-1)",
                            2: "var(--sand-2)",
                            3: "var(--sand-3)",
                            4: "var(--sand-4)",
                            5: "var(--sand-5)",
                            6: "var(--sand-6)",
                            7: "var(--sand-7)",
                            8: "var(--sand-8)",
                            9: "var(--sand-9)",
                            10: "var(--sand-10)",
                            11: "var(--sand-11)",
                            12: "var(--sand-12)"
                        }
                    }
                }
            }
        };
    </script>

    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @stack('dumper')
</head>

<body class="min-h-screen font-sans text-white bg-black">
    <div class="max-w-7xl mx-auto space-y-8">
        <h1 class="block font-bold text-5xl mt-6 text-center text-gray-600">Register</h1>
        <form action="{{ route('register') }}" method="post"
            class="flex flex-col items-center justify-center space-y-4 p-4 md:p-5 border-[1px] border-gray-600 rounded-lg">
            @if(flashMessages.has('errors'))
            <div class="alert alert-danger">
                <ul>
                    @each(error in flashMessages.get('errors'))
                    <li>{{ error.message }}</li>
                    @endeach
                </ul>
            </div>
            @endif
            {{ csrfField() }}
            <div class="w-full">
                <input type="text" name="firstname" value="{{ old('firstname') || '' }}" placeholder="First Name"
                    class="w-full px-4 py-2.5 border-none bg-transparent rounded-lg text-sm" required />
                @inputError("firstname")
                <p class="text-xs text-red-600 mt-2">{{ $messages }}</p>
                @end
            </div>
            <div class="w-full">
                <input type="text" name="lastname" value="{{ old('lastname') || '' }}" placeholder="Last Name"
                    class="w-full px-4 py-2.5 border-none bg-transparent rounded-lg text-sm" required />
                @inputError("lastname")
                <p class="text-xs text-red-600 mt-2">{{ $messages }}</p>
                @end
            </div>
            <div class="w-full">
                <input type="email" name="email" value="{{ old('email') || '' }}" placeholder="Email"
                    class="w-full px-4 py-2.5 border-none bg-transparent rounded-lg text-sm" required />
                @inputError("email")
                <p class="text-xs text-red-600 mt-2">{{ $messages }}</p>
                @end
            </div>
            <div class="w-full">
                <input type="username" name="username" value="{{ old('username') || '' }}" placeholder="Username"
                    class="w-full px-4 py-2.5 border-none bg-transparent rounded-lg text-sm" required />
                @inputError("username")
                <p class="text-xs text-red-600 mt-2">{{ $messages }}</p>
                @end
            </div>
            <div class="w-full">
                <input type="password" name="password" placeholder="Password"
                    class="w-full px-4 py-2.5 border-none bg-transparent rounded-lg text-sm" required />
                @inputError("password")
                <p class="text-xs text-red-600 mt-2">{{ $messages }}</p>
                @end
            </div>
            <div class="w-full">
                <input type="password" name="password_confirmation" placeholder="Confirm Password"
                    class="w-full px-4 py-2.5 border-none bg-transparent rounded-lg text-sm" required />
            </div>
            <button type="submit"
                class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-full">Register</button>
        </form>
        <div class="text-center space-x-3">
            <span class="text-xs">Already have an account ?</span>
            <a href="/auth/login"
                class="underline underline-offset-8 text-lg font-bold text-blue-600 hover:text-blue-700">Login</a>
        </div>
    </div>
</body>

</html>